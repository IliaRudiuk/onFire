import { styled } from '@mui/system';

export const HeaderContainer = styled('div')({
  width: '100%',
  maxWidth: '1170px',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
});
